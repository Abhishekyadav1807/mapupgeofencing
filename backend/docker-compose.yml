services:
  geofencing-api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - DB_PATH=/data/geofencing.db
    volumes:
      - geofencing-data:/data
    restart: unless-stopped

volumes:
  geofencing-data:
